# Requires Python 3.10 for full compatibility on Intel Mac

# Core dependencies
numpy>=1.23.0
ujson>=5.7.0

# Extra features
emoji>=2.8.0
nltk>=3.8.1

# Protocols / API
fastapi>=0.100.0
pydantic>=1.10.0
uvicorn>=0.23.0

# File handling
httpx>=0.24.0

# Language processing
tokenizers>=0.15.0
transformers>=4.30.0

# Development dependencies (install with `pip install -r requirements.txt --extra-index-url` or use a dev requirements file)
black>=23.7.0
flake8>=6.1.0
isort>=5.12.0
mypy>=1.5.0
psutil>=5.9.0
pytest>=7.4.0
questionary>=2.0.1
tqdm>=4.66.0

# Type stubs for development
types-tqdm>=4.66.0
types-ujson>=5.10.0


